services:
  api:
    build: .
    env_file: .env
    ports:
      - "8370:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      TRANSLATEGATE_API_URL: http://api:8000
    ports:
      - "8871:8501"
    depends_on:
      - api
